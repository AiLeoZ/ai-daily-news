# AI 每日资讯 — 云端（GitHub Actions）运行时依赖
# 只包含非纯标准库脚本所需的第三方包；纯 urllib 的脚本（collect.py / generate.py）无需此文件。
# 固定版本以保障跨模型 / 跨环境的二进制产物一致性（海报文字度量 / 抗锯齿 / 二维码生成器行为不变）。
Pillow==10.4.0
qrcode[pil]==7.4.2
# 构建期把 Markdown 预渲染为纯静态 HTML（归档页/速览页零运行时 JS 依赖，避免浏览器端 marked 失败导致白屏）
markdown==3.7
